# 💰 Codebase Reproduction-Cost Estimate

> **Repository:** `cost-estimate`
> **Analysis Date:** 2026-06-15
> **Methodology:** Simplified Parametric Model based on COCOMO II
> **What this estimates:** the cost to **reproduce the current surviving *code*** from scratch —
> not its historical build cost, and **not** the separately-reported intellectual-effort
> artifacts (see Methodology & the disclaimer below).

> ⚠️ **Automated order-of-magnitude estimate — not a professional appraisal.** This figure is
> for internal curiosity only. It is **not** a valuation, fairness opinion, or appraisal, and
> **must not** be used to set prices/invoices or to support acquisition, investment, or legal
> decisions. Reproduction cost ≠ the value of what is owned; counted lines ≠ authored lines.
> No warranty of accuracy — parametric estimates are commonly off by ±50–100%.

> **What this is — and is not (three valuation lenses).** A number on software can mean three very
> different things: **(1) reproduction / replacement cost** *(this tool, the "cost approach")* — the
> parametric cost to rebuild equivalent functionality from scratch; **(2) value / income** — worth
> from revenue, users, or strategic value, *decoupled* from build cost; **(3) market / comparable** —
> comparable-sale prices. A 1,000-line script can underpin a $10M business, and most code has
> near-zero standalone market value — so **reproduction cost ≠ value ≠ price.** (Lenses per IVS
> 2025 / FASB ASC 350-40.)

---

## 🔧 Analysis Environment

| | |
|---|---|
| LOC tool | `cloc` (code-only) |
| jq / git | yes / yes (throwaway history; git effort/cadence N/A < 10 commits) |
| awk math (Monte-Carlo) | yes |
| Monte-Carlo seed | 2050950 (find-derived; reproducible for a given LOC tool) |
| PDF tooling | pandoc yes, xelatex yes |
| OS | Linux |
| Caps / flags hit | **OUT_OF_DOMAIN** (order-of-magnitude only; band → single decade bucket), TINY_REPO, single-author, git effort N/A |

> The headline can shift with tooling: `cloc` (code-only) vs the `wc`×0.7 fallback changes
> KLOC; caps can omit files. **This repo is OUT_OF_DOMAIN, so every figure below is rendered to
> one significant figure (decade band) — treat them as rough buckets, not numbers.**
>
> **Scope:** generated by running the skill on this repository's **shipped files only**
> (`skill/SKILL.md`, `README.md`, `install.sh`, `uninstall.sh`, `LICENSE`, `.gitignore`); the
> internal `docs/` working notes and the `examples/` directory are excluded, so the example does
> not count itself and stays reproducible.

---

## 📊 Codebase Profile

| Metric | Value |
|--------|-------|
| **Languages** | Shell (29) |
| **Total Lines of Code** | 29 (source, code lines, cloc) |
| **Effective KLOC** | 0.03 KLOC (source code only) |
| **Configuration LOC** | 1,638 (Markdown — reported for context, not used in COCOMO) |
| **Total Files** | 6 |
| **Git Commits** | 3 |
| **Contributors** | 1 (mlamp) |
| **Repository Age** | New (same day) |
| **Active Development** | 2026-03-06 → 2026-03-06 |
| **Tech Stack** | Shell/Bash, Markdown documentation, Claude Code skill |

> ⚠️ **Out of COCOMO II's calibrated domain (order-of-magnitude only).** This codebase has under
> 2 KLOC of source code (in fact ~29 lines of shell — the install/uninstall scripts; the tool
> itself is a Markdown skill). COCOMO II is calibrated on 2+ KLOC, multi-person application
> projects, so every figure below is a one-significant-figure bucket, not a precise number.

> **Note:** This codebase is under 500 effective LOC (TINY_REPO). Treat the estimate as rough
> order-of-magnitude only.

> **Note:** Single-author project. COCOMO II assumes multi-person teams, so the team-size and
> schedule framing is indicative only.

## 🔍 Complexity Assessment

| Factor | Score | Justification |
|--------|-------|---------------|
| External Integrations | ⬛⬜⬜⬜⬜ 1/5 | No HTTP/RPC, FFI, or systems-interface idioms in the shell scripts |
| Data Layer | ⬛⬜⬜⬜⬜ 1/5 | No database, ORM, or numerical/array data structures |
| Auth & Authorization | ⬛⬜⬜⬜⬜ 1/5 | No auth, crypto, or concurrency/parser idioms |
| Testing Maturity | ⬛⬜⬜⬜⬜ 1/5 | No test files in the shipped scope |
| Infrastructure/DevOps | ⬛⬜⬜⬜⬜ 1/5 | An install symlink script; no Docker/CI/IaC |
| Error Handling | ⬛⬜⬜⬜⬜ 1/5 | No observability/error-handling patterns |
| Documentation | ⬛⬛⬜⬜⬜ 2/5 | README.md present with usage instructions |
| Security Posture | ⬛⬜⬜⬜⬜ 1/5 | No security patterns found |
| **Average** | **1.1/5** | **Simple** (web and domain probes both ~zero — this is a docs/shell tool) |

## Intellectual Effort Artifacts

Non-code files representing significant domain expertise, prompt engineering, or encoded methodology.

| Classification | Files | Physical Lines | Equiv. Effort LOC | Key Files |
|----------------|-------|---------------|-------------------|-----------|
| Novel Methodology (T4, 3.0x) | 1 | 1,849 | 5,809 | SKILL.md |
| Domain Expertise (T3, 1.5x) | 1 | 145 | 217 | README.md |
| **Total (credited, Tier 2+)** | **2** | **1,994** | **6,026** | |

> ⚠️ **Separate estimate — NOT part of the reproduction-cost headline.** Intellectual-effort
> artifacts contribute **6,026** credited equivalent LOC (Tier 2+ only) ≈ **order-of-magnitude
> 800 hours** (~5 person-months) on their own — **IE effort ≫ the ~4-hour code effort
> here** (this repo is almost entirely a Markdown methodology document; SKILL.md grew with the P2
> redesign, so the IE credit grew with it). This is the **least-grounded** part of the tool, driven
> by its own keyword heuristics, and is **never added to the code-only dollar headline.**
>
> ⚠️ **Conflict of interest (disclosed):** this tool's IE design credits prose/prompt artifacts
> up to 3.0×, and this repo *is* a prose/prompt artifact — so on its own repo the IE figure
> dwarfs the code, which is exactly why IE is kept out of the headline. **This self-referential
> example is illustrative, not a neutral benchmark** (the tool valuing itself). See
> [sample-report-codebase.md](sample-report-codebase.md) for a code-dominated, non-self case.

## ⚙️ Effort Estimation

| Parameter | Value |
|-----------|-------|
| Effective KLOC | 0.03 (Shell source code) |
| Project Type | Simple (avg complexity 1.1/5) |
| Exponent E | 1.10 |
| Effort Multipliers | CPLX=0.73, DATA=0.90, RELY=0.82, PLEX=0.87, DOCU=0.91 |
| EAF (product of 5 mapped EMs; NOT a full COCOMO II EAF) | 0.43 |
| **Source Code Effort (headline; code-only)** | **≈4 hours (0.03 person-months)** |
| Monte-Carlo band | **OUT_OF_DOMAIN → single decade bucket** (~$500; treat P10–P90 as within one decade, $100–$1,000) — not four percentiles at this size |
| Estimated Function Points | ~1 FP (Jones LOC÷ratio — ≈ LOC, not an independent functional count; structural FP is N/A here, see Ensemble) |
| Schedule (Tdev) | ~1 month (ideal; schedule dominates at this size) |
| Implied avg staff | small / part-time effort (implied utilization ~2%) |
| Intellectual Effort (separate, NOT in headline) | ~800 hours on its own — see the Intellectual Effort Artifacts section |

## 📐 Estimator Ensemble (cross-checks — none move the headline)

| Estimator | Size basis | Effort estimate |
|-----------|-----------|-----------------|
| **COCOMO II** (headline anchor) | Effective KLOC | ≈4 h (~$500, order-of-magnitude) |
| IFPUG FP → effort (P2-1) | structural UFP | **N/A — not a transaction-oriented codebase** (2 shell scripts; no routes/entities) |
| COSMIC CFP (P2-2) | data movements | **N/A — not transaction-oriented** |
| Use-Case Points (P2-10) | actors + use-cases | **N/A — no use-case surface** (no routes/handlers/CLI subcommands) |
| Putnam SLIM (P2-9) | — | citation-only (no number; unreliable far below its calibrated range) |
| Backfired FP (≈LOC) | LOC ÷ gearing | ~1 FP; **≈ LOC in disguise, not independent** |

> Every structural cross-check is **N/A** here — this is a docs/shell tool, not a transaction app
> with a use-case surface — so there is nothing to corroborate COCOMO against; the headline rests on
> COCOMO alone (and is order-of-magnitude at 0.03 KLOC anyway). No "convergence → confidence" claim.

## 📏 Measured vs Parametric (git-history anchor — SEPARATE, never in the headline)

> Measured git-effort: **N/A** — the throwaway history has only 3 commits (below the 10-commit
> floor) and ~0 days of age. (On a real repo with ≥10 commits over ≥14 days this section reports
> git-reconstructed person-hours, the AI-authorship share from `Co-Authored-By` trailers, and the
> demonstrated commit cadence — a *relative*, unvalidated anchor, never folded into the headline.)

## 💵 Cost Estimation

### By Team Configuration (rate sensitivity — NOT the uncertainty band)

Cost = code-only point Person-Hours × Blended Rate (no overhead multiplier). All figures are
order-of-magnitude (1 significant figure):

| Team Profile | Cost (point) | Calendar Time |
|-------------|--------------|---------------|
| 👤 Solo Senior ($125/hr) | ~$500 | ~1 month |
| 🚀 Lean Startup ($115/hr) | ~$400 | ~1 month |
| 📈 Growth Co ($125/hr) | ~$500 | ~1 month |
| 🏢 Enterprise ($145/hr) | ~$600 | ~1 month |

> Profiles differ only by rate. Tdev dominates at this size, so all profiles show ~the same
> minimum schedule (~1 month).

### Effort Allocation (code-only point estimate; ~4 hours total — illustrative at this scale)

| Activity | % (generic default) | Hours |
|----------|---------------------|-------|
| Development & Coding | 55% | ~2 h |
| Testing & QA | 20% | ~1 h |
| Project Management | 12% | ~0.5 h |
| DevOps & Infrastructure | 8% | ~0.3 h |
| Documentation | 5% | ~0.2 h |

## 🏷️ Headline: Reproduction-Cost Estimate (cost approach)

> **Estimated cost to reproduce the current *code* from scratch** (a parametric replacement-
> rewrite of the ~29 surviving shell lines — the *cost approach*, **not** value or market price;
> not the historical build cost, and **not** the separately-reported intellectual-effort
> artifacts):
>
> **~$500** (deterministic point) &nbsp; — ⚠️ **Order-of-magnitude only**: this repo is ~29 LOC of
> code (the tool is a Markdown skill), far outside COCOMO II's calibrated domain, so the figure is
> a **single decade bucket** ($100 – $1,000), not a precise number or a four-percentile band. A
> small / part-time effort over ~1 month.
>
> Intellectual-effort artifacts are **~800 hours** on their own, reported separately and **not**
> in this figure (see that section). On this repo, that prose/prompt credit is ~200× the code
> effort — a vivid illustration of why it is kept out of the headline.
>
> _Reproduction cost, not value or price — not an appraisal; see the disclaimer at the top._

## 🤖 AI Speed Comparison (illustrative, both inputs unverified)

> ⚠️ This is **not a controlled measurement.** It divides a **self-reported** AI build time by the
> model's **hypothetical** human estimate — neither number is measured. Independent evidence is
> mixed-to-negative: the **METR RCT** (arXiv:2507.09089, Jul 2025; 16 experienced OSS devs, 246
> tasks on mature repos) found AI made them **~19% slower** while they *felt* ~20% faster (point
> estimate); **DORA 2024** (correlational) linked each +25% AI adoption to **−1.5% throughput** and
> **−7.2% delivery stability** (alongside +7.5% docs); **GitClear 2025** (211M LOC) reports
> refactoring fell **25%→<10%** and clones rose 8.3%→12.3%. The one large *positive* — an early
> GitHub Copilot lab study, **~55% faster** — was an *isolated greenfield* task (95% CI 21–89%).
> Read the ratio as anecdote, not a measured speedup.

| Metric | Human Team (modeled, unverified) | AI-Assisted (self-reported, unverified) |
|--------|-----------|-------------|
| Build Time | ≈4 hours | 3 hours |
| Ratio | 1× | ≈1× (order-of-magnitude) |
| Cost ($125/hr, no overhead) | ~$500 | ~$400 |

> The human baseline is the **code-only** point estimate (≈4 hours); intellectual-effort artifacts
> are **excluded** by design (they are not a "build"). A self-reported AI time of **3 hours** gives
> a ratio of about **1×** — illustrative only, not a measured speedup. At this scale the human and
> AI times are comparable: the code itself is tiny (~29 shell lines), and the bulk of this repo's
> effort is the separately-reported (non-code) intellectual-effort artifacts, which this comparison
> excludes.

## 📋 Methodology & Assumptions

- **What it estimates (reproduction cost):** KLOC is measured from the **present file snapshot**; git history is collected for context but does not enter the headline math. So this estimates the parametric cost to **reproduce the surviving code**, not its historical build cost — it omits deleted/reworked/abandoned code and rewards verbose implementations. The historical effort was typically **higher**.
- **Three valuation lenses (P2-14):** this is the **cost approach** (reproduction/replacement cost), distinct from **value/income** and **market/comparable** — a low-cost artifact can have high value and vice-versa (IVS 2025; FASB ASC 350-40).
- **Model:** Simplified parametric model based on COCOMO II (Boehm et al., 2000): base equation (A=2.94, exponent E from project class) with **5 of the 17 Effort Multipliers** mapped; the other 12 default to Nominal (1.0). The displayed "EAF" is **not a calibrated COCOMO II EAF**. Exponents are kept inside COCOMO II's real range (Simple E=1.10, Moderate 1.16, Complex 1.22).
- **How accurate is this, really? (P2-5):** *uncalibrated* parametric models score ≈ **MMRE 1.0 (~100% mean error)** and **PRED(25) ≈ 0** on real datasets; we cannot report this repo's MMRE/PRED (n = 1). The figures are a property of the method class and are why the band is wide — and at 0.03 KLOC only a bucket is honest.
- **Uncertainty band (Monte-Carlo, P2-6):** the headline is the **deterministic point**; in-domain the range is a seeded 10,000-iteration Monte-Carlo P10–P90 over triangular size + productivity inputs. **Here (OUT_OF_DOMAIN) the four-percentile band is suppressed and collapsed to a single decade bucket.** Replaces the old invented ×0.5–×2.0 band.
- **Estimator ensemble (P2-1/2/9/10):** structural FP/COSMIC/UCP run as archetype-gated cross-checks — all **N/A** here (no transaction/use-case surface). SLIM is citation-only. **None move the headline**; no "convergence → confidence" claim.
- **Measured vs parametric (P2-11/13):** N/A here (throwaway history < 10 commits); on a real repo a git session reconstruction + AI-provenance share + commit cadence are reported as a relative, unvalidated anchor, never in the headline.
- **Headline is code-only; intellectual effort is SEPARATE:** the reproduction-cost figure is COCOMO on source code only. Config/markup LOC (the Markdown here) is excluded from KLOC and reported separately. Intellectual-effort artifacts are estimated on their **own** line and **never** summed into the headline, cost tables, schedule, or Total FP. A file is either *code* (COCOMO) or a *non-code artifact* (IE), never both.
- **Rates & overhead:** fully-loaded US consulting/agency rates (2025-2026). **No overhead multiplier** — a fully-loaded rate already bills hiring/coordination/compliance, and COCOMO's `KLOC^E` already encodes size diseconomies. Team size is a **schedule** effect, not a cost markup.
- **Intellectual Effort model:** non-code artifacts get a 5-tier classification (T0 0× … T4 3.0×) from a **stuffing-resistant** signal model — tiers come from distinct capped trigger-lemma *richness* + family *breadth* + surrounding-vocabulary *support* (not raw keyword frequency), and credited equivalent-LOC is **token-based and support-capped** so repetition, line-wrapping, padding, and file-splitting cannot inflate it. **All tier multipliers, thresholds, and the IE→FP divisor are heuristics chosen by the authors, not empirically calibrated.**
- **Conflict of interest (disclosed):** this tool's IE design credits prose/prompt artifacts (up to 3.0×); on this prose-heavy repo IE dwarfs the code, which is why IE is kept out of the headline. The self-referential example is **illustrative, not a neutral benchmark**; a code-dominated calibration example ([sample-report-codebase.md](sample-report-codebase.md)) is shipped for a non-self comparison.
- **Bias direction (honest):** no overhead multiplier and no cherry-picked scenario tiers. The headline is a single mid-rate ($125), code-only **deterministic point**; the only range is the Monte-Carlo band (here a decade bucket). The structural cross-checks, the measured git-effort anchor, and the IE credit are reported separately and never enter the headline; only a genuine user-supplied actual (Bayesian, capped) moves it.
- **Working Hours:** 152 hours/month. **Schedule:** Tdev = 3.67 × (code-only PM)^F, F = 0.28 + 0.2×(E−0.91); Calendar Time = max(naive hours/headcount/152, Tdev). Implied headcount = software PM / Tdev; the "team of N" framing is suppressed here (implied utilization ~2% < 25%). The 55/20/12/8/5 activity split is a generic industry default, not measured.
- **Small project / single-author caveats:** model calibrated for 2+ KLOC multi-person projects; this is ~29 LOC, single-author — order-of-magnitude only.

*Sources: Boehm, B. et al. (2000). Software Cost Estimation with COCOMO II. Prentice Hall. Jones, C. Applied Software Measurement, 3rd ed. McGraw-Hill (LOC/FP ratios only). Function points: IFPUG CPM 4.x / ISO/IEC 20926; COSMIC ISO/IEC 19761; UCP (Karner 1993). Uncertainty: PMI PMBOK three-point/Monte-Carlo; triangular. Accuracy bounds: Jeklin/Saad/Ekawati (2025), Clark/Chulani/Boehm (ICSE 1998). Valuation lenses: IVS 2025, FASB ASC 350-40. Git effort: git2effort (arXiv:2203.09898). Rates: ZipRecruiter, FullStack Labs, Rise 2026 Contractor Rates. AI-productivity evidence: METR RCT (arXiv:2507.09089, Jul 2025); DORA 2024; GitClear 2024/2025.*

---

*Generated by [cost-estimate](https://github.com/mlamp/cost-estimate) -- a Claude Code skill using a simplified parametric model based on COCOMO II*

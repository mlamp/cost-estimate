# stacklang README

A tiny stack-based bytecode language: lexer, recursive-descent parser,
bytecode compiler, and a stack VM. See `examples/` for sample programs.

Usage: `python main.py run examples/fib.stk`
